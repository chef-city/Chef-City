%html
%head
  %title= yield(:title) || 'Chef City'
  = stylesheet_link_tag    "application"
  = javascript_include_tag "application"
  = csrf_meta_tags

%body
  = render :partial=>'shared/header'
  = yield